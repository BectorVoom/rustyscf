# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.87"
name = "tracel-llvm"
version = "20.1.4-5"
authors = ["Tracel Technologies Inc."]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Rust bindings for LLVM libraries used by CubeCL."
readme = "README.md"
keywords = [
    "cubecl",
    "llvm",
    "mlir",
    "bindings",
]
categories = [
    "api-bindings",
    "external-ffi-bindings",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/tracel-ai/tracel-llvm/tree/main/crates/tracel-llvm"
resolver = "2"

[features]
mlir-helpers = ["tracel-mlir-rs/helpers"]
mlir-ods-dialects = ["tracel-mlir-rs/ods-dialects"]

[lib]
name = "tracel_llvm"
path = "src/lib.rs"

[dependencies.tracel-mlir-rs]
version = "20.1.4-5"

[dependencies.tracel-mlir-sys]
version = "20.1.4-5"
